import React from 'react';

export default function EmptyControls() {
    return (
        <div className="sessions-query-empty">
            This visualization uses the global filters only.
        </div>
    );
}
